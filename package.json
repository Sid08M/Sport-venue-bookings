{
  "name": "sports-os-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "seed": "npx tsx packages/db/seed.ts"
  },
  "devDependencies": {
    "turbo": "^2.9.12"
  }
}
