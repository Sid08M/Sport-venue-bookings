{
  "name": "sports-os-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@11.1.1",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npx cross-env NEXT_DISABLE_LOCKFILE_PATCH=true turbo run dev",
    "build": "npx cross-env NEXT_DISABLE_LOCKFILE_PATCH=true turbo run build",
    "seed": "npx tsx packages/db/seed.ts",
    "studio": "npm run studio --workspace=@repo/db"
  },
  "devDependencies": {
    "turbo": "^2.9.14"
  }
}
